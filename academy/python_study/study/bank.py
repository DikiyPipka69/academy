import matplotlib.pyplot as plt

clients = {'дмитрий':120000, 'анна':10000, 'иван':70000}

class Finances:
    def __init__(self):
        self
    def add_client(clients):
        # добавляет клиента
        clients.update({add_name:add_balance})
        return clients
    def delete_client(clients,temporary_client):
        # удаляет клиента
        clients.remove(remove_client)
    def replenish_balance(clients):
        # меняет баланс клиента
        pass
    def credit_system():
        # кредитная система
        pass

print('добавить клиента(add)/удалить клиента(re)')
user_choice = input('> ')
if user_choice == 'add':
    add_name = input('введите имя > ')
    add_balance = int(input('введите баланс > '))
    print(Finances.add_client(clients))
elif user_choice == 're':
    remove_client = input('введите клиента которого надо удалить > ')
    print(Finances.delete_client(clients))












































































































